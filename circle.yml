machine:
  java:
    version: oraclejdk8

 dependencies:
  override:
    - chmod +x gradlew
    - ./gradlew dependencies
 test:
  override:
    - chmod +x grailsw
    - ./grailsw test-app --non-interactive